import Link from "next/link";
import { listProducts } from "../../lib/medusa";

export const dynamic = "force-dynamic";

export default async function PromocoesPage() {
  const all = await listProducts();
  const promo = (all || []).filter((p: any) => p?.metadata?.promocao === "semana");

  return (
    <main className="container py-12">
      <div className="flex items-center justify-between gap-4 mb-10">
        <h1 className="font-display text-4xl font-bold">
          Promoções <span className="text-gradient-gold">da semana</span>
        </h1>
        <Link href="/catalogo" className="text-sm font-bold text-primary">
          Ver catálogo
        </Link>
      </div>

      {promo.length === 0 ? (
        <div className="steel-card p-6 text-muted-foreground">
          Nenhuma promoção ativa no momento.
        </div>
      ) : (
        <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
          {promo.map((p: any) => (
            <Link key={p.id} href={`/produto/${p.handle}`} className="steel-card steel-card-hover p-5 block">
              <div className="font-bold">{p.title}</div>
              <div className="text-sm text-muted-foreground mt-2">Ver detalhes</div>
            </Link>
          ))}
        </div>
      )}
    </main>
  );
}



